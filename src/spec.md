# Specification

## Summary
**Goal:** Update the application name in the header to "TestAut".

**Planned changes:**
- Change the application name displayed in the header navigation from the current name to "TestAut"
- Update applies to the Layout component's logo/brand link element

**User-visible outcome:** The header will display "TestAut" as the application name across all pages.
